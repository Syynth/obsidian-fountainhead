import {FountainheadSettings} from "~/types";

export const PLUGIN_TYPE = 'STATUS92/FOUNTAINHEAD';

export const DEFAULT_SETTINGS: FountainheadSettings = {
  isCool: true,
  testString: 'Some stuff',
}
